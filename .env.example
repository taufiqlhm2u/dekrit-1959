# .env.example — Copy this to .env.local for local development
# DO NOT commit .env.local to Git

NODE_ENV=production

# Optional: Set a custom port (cPanel Passenger overrides this automatically)
# PORT=3000

# Add your application-specific environment variables below:
# NEXT_PUBLIC_SITE_URL=https://app.yourdomain.com
